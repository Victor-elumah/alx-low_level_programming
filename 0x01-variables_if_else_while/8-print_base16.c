#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0
 */

int main(void)
{
	char c;

	int d;

	c = 'a';
	while
		(d < 10); {
			puthcar(d + '0');
			d++;
		}
	while
		(c <= 'f'); {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
